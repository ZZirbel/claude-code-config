use anyhow::Result;

pub fn run(_ways_dir: Option<String>, _output: Option<String>) -> Result<()> {
    eprintln!("ways graph: not yet implemented");
    eprintln!("  New subcommand (pure Rust, Phase 1)");
    Ok(())
}
