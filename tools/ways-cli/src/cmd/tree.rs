use anyhow::Result;

pub fn run(_path: String, _jaccard: bool) -> Result<()> {
    eprintln!("ways tree: not yet implemented");
    eprintln!("  Will replace: tools/way-tree-analyze.sh");
    Ok(())
}
