use anyhow::Result;

pub fn run(_query: String, _corpus: Option<String>) -> Result<()> {
    eprintln!("ways match: not yet implemented");
    eprintln!("  Will replace: way-match binary (C FFI, Phase 2)");
    Ok(())
}
