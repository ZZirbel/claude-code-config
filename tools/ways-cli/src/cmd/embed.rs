use anyhow::Result;

pub fn run(_query: String, _corpus: Option<String>, _model: Option<String>) -> Result<()> {
    eprintln!("ways embed: not yet implemented");
    eprintln!("  Will replace: way-embed binary (C++ FFI, Phase 2)");
    Ok(())
}
