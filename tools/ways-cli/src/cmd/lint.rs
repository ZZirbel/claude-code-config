use anyhow::Result;

pub fn run(_path: Option<String>, _schema: bool, _check: bool) -> Result<()> {
    eprintln!("ways lint: not yet implemented");
    eprintln!("  Will replace: hooks/ways/lint-ways.sh");
    Ok(())
}
