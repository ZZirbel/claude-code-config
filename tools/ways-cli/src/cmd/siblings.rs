use anyhow::Result;

pub fn run(_id: String, _threshold: f64, _corpus: Option<String>, _model: Option<String>) -> Result<()> {
    eprintln!("ways siblings: not yet implemented");
    eprintln!("  New subcommand (C++ FFI, Phase 2)");
    Ok(())
}
